package core.element.widget.calendar;

import core.common.Identifier;

/** Week of the CalendarList widget (the button "Week"): the events of the week are placed by hours. */
public class PlatformCalendarWeekWidget extends CalendarListWidget<PlatformCalendarWeekWidget> {

	public PlatformCalendarWeekWidget(Identifier identifier, String textIdentifier) {
		super(identifier, textIdentifier);
	}

}
