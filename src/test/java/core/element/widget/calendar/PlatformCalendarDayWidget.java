package core.element.widget.calendar;

import core.common.Identifier;

/** Day of the CalendarList widget (the button "Day"): the events of the day are placed by hours. */
public class PlatformCalendarDayWidget extends CalendarListWidget<PlatformCalendarDayWidget> {

	public PlatformCalendarDayWidget(Identifier identifier, String textIdentifier) {
		super(identifier, textIdentifier);
	}

}
