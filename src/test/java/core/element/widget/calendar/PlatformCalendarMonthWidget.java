package core.element.widget.calendar;

import core.common.Identifier;

/** Month of the CalendarList widget (the button "Month"), the widget is opened with it. */
public class PlatformCalendarMonthWidget extends CalendarListWidget<PlatformCalendarMonthWidget> {

	public PlatformCalendarMonthWidget(Identifier identifier, String textIdentifier) {
		super(identifier, textIdentifier);
	}

}
